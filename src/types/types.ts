export type MemeTemplate = {
  id: string;
  name: string;
  src: string;
  alt: string;
};

export type VibeCategories = {
  id: string;
  label: string;
  description: string;
  tooltip: string;
};

export type ToneOptions = {
  id: string;
  label: string;
  description: string;
  tooltip: string;
};
